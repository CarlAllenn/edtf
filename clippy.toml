# msrv mirrors [workspace.package] rust-version — same single source of truth
# the MSRV CI job derives from; a drift between the two is a bug.
msrv = "1.82"
# Pre-1.0: API-breaking clippy suggestions are wanted, not suppressed. Flip to
# true at v1 — semver honesty is cargo-semver-checks' job, not clippy's.
avoid-breaking-exported-api = false
